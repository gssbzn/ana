development:
  adapter: mysql
  encoding: utf8
  database: anaerp_development
  username: anaerp
  password: anaerp
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  encoding: utf8
  database: anaerp_test
  username: anaerp
  password: anaerp
  host: localhost

production:
  adapter: mysql
  encoding: utf8
  database: anaerp_production
  username: anaerp
  password: anaerp
  host: localhost
